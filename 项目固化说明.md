# TeslaCanPython 项目固化说明

**封版日期：** 2026-09-01  
**封版范围：** TM3公共诊断报告链路与TM3-009/010 Golden Regression  
**Golden Regression：** PASS

## 1. 当前项目阶段

项目处于Model 3实车采集、基线建立、诊断分析方法校正阶段，采用**实验驱动，而不是知识库驱动**。当前重点是继续基线采集、稳定分析结论与下一步建议，并通过案例修正Evidence Plan能力；正式知识库与自动诊断体系待方法稳定后再建设。

## 2. 当前有效规则与规范

- 工作区长期规则：`AGENTS.md`。
- 当前有效诊断规范：`L3新能源实车数据诊断分析规范_v1.1.md`。
- 历史规范：`L3新能源实车数据诊断分析规范_v1.0.md`，保留用于版本追溯，不覆盖。
- Golden登记：`doc/methodology/TM3_Golden_Regression_Samples.md`。

长期方法：系统理解是基础，通信Signal是路径，逻辑推理是方法，控制树是目标。控制树定义关系，Signal提供证据；实验目标、控制器内部目标和物理结果必须区分。

## 3. 公共程序链路

`Draft Evidence Plan → Human Review/Override → Approved Evidence Plan → Evidence Assessment → Report View Model → Renderer`

- `src/evidence_plan.py`：Draft、uncertainty、Review Override、Approved校验和Assessment状态。
- `src/can_evidence_utils.py`：过去最近样本、Signal age、同帧扭矩比较、Pack同帧V×I、连续窗口、DBC可读性及ASC hash。
- `src/report_renderer.py`：只消费Approved/effective计划和上游View Model；渲染最终报告、时间线、Signal附件和工程审计。
- `src/render_tm3_regression.py`：用冻结输入重建TM3-009/010公共产物。

`ControlRelationshipView`由上游提供控制链节点及观测状态、动力/物理响应、能源交叉验证和边界。Renderer不从Signal列表推导控制关系。

## 4. Golden Samples

- TM3-009：中等负载加速及独立低速制动交互边界。
- TM3-010：约20/40 km/h稳定匀速；20 km/h有效、40 km/h未建立稳态，仅补采40 km/h。

Golden保护结论、窗口、关键数值、Signal语义、控制关系表达、人读结构、Evidence Plan审核机制和Renderer职责。没有真实错误证明时不得静默修改。

## 5. 测试保护范围

- `test_evidence_plan.py`：不确定项审核阻断、建议/Override并存、实验级scope。
- `test_can_evidence_utils.py`：过去样本、Signal age、同帧比较/功率、连续窗口、可读性与hash。
- `test_report_renderer.py`：Approved入口、effective排序、未批准Signal拒绝、固定时间线、控制关系独立View、缺失直接Signal时节点保留、三条关系独立、浮点尾数与模板标点。
- `test_tm3_regression_outputs.py`：009/010四份产物、冻结结论、目标车速边界、Signal级用途、人读结构与数值格式。

封版时共19项unittest通过。

## 6. 新Codex会话读取与工作顺序

1. 读取`AGENTS.md`；
2. 读取当前规范`L3新能源实车数据诊断分析规范_v1.1.md`；
3. 读取本固化说明和Golden登记；
4. 查看公共代码、tests和相关Golden产物；
5. 明确新实验用途、目的、系统边界、主线、控制树片段和状态机；
6. 生成待审Evidence Plan，人工审核后才分析和渲染；
7. 输出明确结论与最小下一步，并运行全部Golden Regression。

不依赖历史聊天才能恢复上述方法。

## 7. 已冻结与仍在探索

已冻结：TM3-009/010验收结果、公共链路、Renderer无诊断权、固定人读骨架、Assessment状态集合和实验级审核原则。

仍在探索：后续TM3实验的控制树片段、状态机适用性、候选Signal语义、充分性判据、不同工况基线和跨实验可复用知识。人工审核只对当前实验负责。

## 8. 当前明确暂停

- 正式L3知识库；
- 全量Model 3 Signal Semantic Mapping数据库；
- 自动Evidence Plan编译器和Signal评分器；
- 自动控制树生成与自动故障原因确认；
- 审核GUI；
- Notion自动同步成程序规则；
- 全历史实验迁移。

本次封版不包含TM3-011分析或开发。
