# 第三轮修订验证记录

- 输入范围：`evidence_vault/historical_cases` 下 S0014–S0018 五份 HVAC MinSingRound。
- 会话完整性：5/5 `COMPLETED`，事件 E01–E07 均为 `triggered`，无备注。
- CAN 完整性：5 个 ASC，共 2,592,668 帧；解析到的时间范围均覆盖约 0–230 s；无倒退时间戳、无无效帧行。
- 外部事实：五轮 E03/E05 照片均人工核验为 HVAC UI ON/OFF 方向一致。
- 稳态窗口：逐会话使用实际事件时间，并在操作点两侧设置保护区。
- 第一层门槛：每轮 OFF 前＝OFF 后≠ON，三个窗口纯度均 ≥95%，且五轮方向一致。
- 第一层结果：52 个跨轮 bit 候选。
- 第二层门槛：五轮中每轮全程均仅出现预期的开启、关闭两次翻转，且两次转换均在事件附近找到。
- 第二层结果：23 个跨五轮复现的二态 raw bit，覆盖 10 个 CAN ID；不解释为23个独立 Signal。
- 转换方向：开启和关闭匹配均显式记录 before/after；23个复现 raw bit 的115组会话记录方向全部匹配。
- 字段回收：23个 raw bit 按 CAN ID+字节归为16个字节级待恢复字段组；这是边界假设，不是16个已解码 Signal。
- 历史复核：第二轮 5 个优先候选全部 5/5 通过；所有稳态窗口纯度 100%；每轮全程均仅两次翻转。
- 证据表述：取消未定义的C+/B字母评级；改为“由单周期相关提升为五个单独同条件会话均复现；具体语义未确认”。
- 时延限制：ASC 文件头为整秒精度；近事件组只称为约±1秒精度内近同时，不比较亚秒先后；只保留近事件与数秒后两个粗粒度层级。
- 审计状态：最终 M Re-Audit 已通过，Round 3 状态为 `CLOSED`。
- 结果隔离：所有新产物写入 `results/round_3/`；`results/round_2/` 与 `work/round_2/` 未改写。
