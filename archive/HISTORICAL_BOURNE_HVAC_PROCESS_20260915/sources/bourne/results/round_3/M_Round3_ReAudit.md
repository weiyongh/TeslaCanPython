# 伯恩 Revised Round 3 — M Re-Audit

## 1. Re-Audit边界

- 本次复审以原《伯恩第三轮纯审计意见》及Round 3 Rework Gate为依据。
- 复审对象为伯恩实际提交的修订报告、验证记录、自审记录、机器结果、处理记录和分析代码。
- 伯恩的自审声明不作为整改完成的证明；所有关键计数、方向字段、分组关系和产物表述均按实际文件交叉核对。
- 本次只裁决Round 3，不提供外部答案，不判断候选最终含义，不设计下一步Acquisition Plan。

## 2. 已核验通过的整改

| 原Audit要求 | 实际核验 | 裁决 |
|---|---|---|
| 收窄“23个强候选” | README与VALIDATION均改为“23个跨五轮复现的二态raw bit”，并明确不等于23个独立Signal或Evidence | `PASS` |
| 从单bit回到待恢复字段组 | 新增`raw_field_groups.csv`；23个入选bit完整归入16个字节级假设组，无遗漏、无额外成员 | `PASS_WITH_BOUNDARY` |
| 避免把字节组写成已恢复Signal | README与CSV均明确`byte_level_hypothesis_only`，并说明不能确定跨字节边界 | `PASS` |
| 显式核对转换方向 | 代码按预期before→after筛选；CSV保存期望方向、实际前后值和匹配结果 | `PASS` |
| 核验115组方向声明 | 23 bit × 5会话=115条入选记录；115条开启与关闭方向全部匹配，且全程翻转数均为2 | `PASS` |
| 修正亚秒时延表达 | 人读报告不再比较近事件组内部亚秒顺序；原始偏移只保留在机器表中供复核 | `PASS` |
| 收窄会话独立性 | 已改为“五个单独采集会话中的同条件重复验证” | `PASS` |
| 取消未定义字母等级 | README与VALIDATION取消C+/B结论，改用直接Evidence描述 | `PASS` |
| 区分已证明事实与未确定解释 | README分别设置“已由当前数据证明的事实”和“当前数据仍不能确定的解释” | `PASS` |
| 当前Round状态门 | README、自审和VALIDATION均写明等待M Re-Audit且本轮不提交下一步采集计划 | `PASS_IN_PRIMARY_RESULTS` |

## 3. 机器结果一致性核验

实际CSV交叉核对结果：

```text
跨轮稳态候选：52
跨五轮复现的二态raw bit：23
候选转换记录：260 = 52 × 5
入选bit方向记录：115 = 23 × 5
115条开启方向：全部匹配
115条关闭方向：全部匹配
115条全程翻转数：全部等于2
字节级待恢复字段组：16
字段组成员总数：23
多成员组：7
单成员组：9
字段组与23个入选bit集合：完全一致
```

README、VALIDATION、`cross_round_bit_candidates.csv`、`candidate_transitions.csv`、`raw_field_groups.csv`和自审记录中的上述核心数字一致。

字段组方法只按CAN ID与所在字节形成边界假设，没有证明完整字段边界。伯恩已在报告中明确这一限制，并列出跨字节边界仍无法由当前数据可靠确定，因此没有把16组错误升级为16个Signal。

## 4. 仍未通过的Gate

### 4.1 处理记录仍提前指定下一步验证路径

`work/round_3/process_session.md`的“结论边界”仍写道：

> 只有完成送风/A/C解耦并引入物理执行证据后，才可尝试具体语义命名。

这不是单纯记录“当前Evidence不足”，而是在实际修订产物中保留了一条具体的后续条件组合与验证路径。它与同一文件上文所写的“暂停下一轮采集计划”、README的“本轮不提交下一步采集计划”以及本轮Rework Gate不一致。

本轮Gate要求所有修订产物停止在：

```text
当前数据做过什么检查
为什么仍无法区分
剩余Evidence Gap是什么
```

伯恩已经在README的剩余不确定性表中完成了这一层表达，不需要也不应在处理记录中继续给出具体下一步方案。

### 4.2 所需返工范围

本次不要求新增采集、不要求提出下一轮计划，也不推翻已经核验通过的52/23/115/16结果。

伯恩只需在当前Round内：

- 清除`work/round_3/process_session.md`中提前指定具体后续验证路径的句子；
- 将该处收敛为当前数据仍不能确定什么、为什么不能确定以及剩余Evidence Gap；
- 再次检查README、VALIDATION、自审、处理记录和归档清单对“Round 3-only、等待M复审、不进入下一轮”的表述完全一致。

## 5. 最终裁决

```text
Round 3：REWORK_REQUIRED
```

核心数据与主要整改已经通过复核，但Round 3实际产物中仍残留具体下一步验证路径，违反当前Rework Gate并造成产物状态不一致。在该处完成最小返工并重新提交前，Round 3不能标记为CLOSED。
