# HVAC Round 4 第3次预审整改意见

## 一、预审结论

第2次预审要求的Event追溯、时间基准、方法定型和实现引用清理已经完成。当前正式产物的技术内容和归档指纹核对通过，但部分文件仍包含完成本次提交不需要的预审安排和范围外信息。

本次预审结论：

`PREAUDIT_REWORK_REQUIRED`

当前状态继续保持：

`REVISED / PENDING_RE-AUDIT`

## 二、已经完成的事项

以下事项不再要求返工：

1. `event_evidence_index.csv` 共65行，`session_id + event_id` 键唯一，Event状态完整。
2. 六个稳态窗口均可由逐Event时间按照 `METHOD.md` 的规则重建，并与 `timeline_reconstruction.json`、`session_quality.csv` 一致。
3. Event关联照片合计31张，与 `photo_evidence_index.csv` 一致；S0023补拍绑定E08且不改变分析窗口。
4. 每个Session的Event源文件指纹在输入清单和Event索引之间一致。
5. `input_evidence_manifest.csv` 已明确区分相对脚本时间与带时区墙钟时间。
6. `METHOD.md` 已补齐bit编号、并列模态、空窗口、无效记录、数据长度、候选去重、输出排序及转换匹配规则。
7. Session、Event、照片、帧数、候选组、raw bit、分类和转换记录数量闭合。
8. `archive_manifest.md` 所列正式文件SHA-256均与当前文件一致。
9. 第2次整改没有改变既有分类和核心结论；发生变化的组明细文件已在整改答复中说明原因。

## 三、必须整改的问题

### 1. 删除不属于当前提交责任的预审安排信息

正式提交物只需记录当前状态，不需要记录预审通过次数、下一份预审文件编号或后续预审安排。

请删除以下内容：

- `README.md` 中的“预审通过轮次”；
- `VALIDATION.md` 中的“预审通过0轮”；
- `SELF_REVIEW.md` 中的“已通过的预审”和“下一预期意见编号”；
- `REWORK_RESPONSE.md` 中的“预审通过0轮”；
- `archive_manifest.md` 中的“预审通过0轮”。

可以保留：

- 已完成哪些整改；
- 对应的预审意见文件；
- 当前状态 `REVISED / PENDING_RE-AUDIT`；
- 提交后停止并等待复审。

### 2. 收窄 `METHOD.md` 的输入边界表述

`METHOD.md` 的输入边界只需正面列出本轮实际采用的输入。请删除对未采用来源、外部范围和既有答案的否定性说明。

建议保留为事实性表达：

> 本轮分析采用S0019–S0023的Session记录、Event记录、总线记录和照片，以及Round 3正式提交的16个既有候选组。

不要增加其他背景说明。

### 3. 收窄 `VALIDATION.md` 的可复现摘要

“可复现摘要”只需说明正式输入、方法合同和预期摘要之间的对应关系，不需要描述正式复核采用什么材料，也不需要说明未提交什么实现。

请将相关段落改为直接的产物说明，例如：

> `METHOD.md` 记录确定性方法合同；输入Evidence索引记录冻结输入标识；下列摘要记录本次正式结果的关键数量。

不得借此修改预期摘要中的现有数值。

### 4. 同步自审、整改答复和归档指纹

完成上述定点删除后：

- 更新 `SELF_REVIEW.md`，只保留本次提交前的自审责任和结果；
- 更新 `REWORK_RESPONSE.md`，记录第3次预审的整改位置和文件指纹变化；
- 更新 `archive_manifest.md` 中受影响文件的SHA-256；
- 核对顶层状态入口与Round 4当前状态一致。

## 四、整改边界

本次只进行上述定点删除和相应指纹更新。

不得借机修改：

- 已冻结的机器统计结果；
- 稳态窗口；
- 候选分类；
- 数量摘要；
- Evidence结论边界；
- 已放行的照片处置。

如定点修改意外影响上述内容，必须在 `REWORK_RESPONSE.md` 中说明。

## 五、状态与停止点

整改完成后，状态保持：

`REVISED / PENDING_RE-AUDIT`

提交后停止，等待复审。不得自行记录预审通过、正式审核通过、`CLOSED`或下一阶段状态；不得执行新的采集。
