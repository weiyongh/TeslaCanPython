适用脚本：L3-HVAC-ModeFanAC-FieldDisambiguation_单人多状态单轮采集脚本.txt

执行要求：完整执行5个单独采集会话，每轮从00s运行至330s并单独保存。

轮间HVAC全关至少60秒。五轮使用同一车辆、接口、通道、手动20°C、面部出风和固定内循环。开始时环境显示温度应不低于25°C；不满足时记录实际温度，数据仍可用于UI/CAN解耦，但不得用于确认物理制冷响应。

每轮必须获得以下六种状态、E01–E13完整事件记录和六张确认照片：初始全关、仅送风风量2、仅送风风量6、风量6并开启A/C、关闭A/C后保持风量6、最终全关。

三个仅送风确认点必须确认A/C关闭；“风量6并开启A/C”确认点必须确认A/C开启。

若自动模式介入、A/C状态不符、发生额外车辆操作、CAN中断或关键照片缺失，该轮标记无效并重采，不得用其他轮结果替代。

若有出风口温度计，开始前固定探头并在整轮保持位置不变，让六张确认照片同时包含读数；若无温度计，本批数据只能用于CAN/UI字段解耦，不能确认实际制冷建立。
