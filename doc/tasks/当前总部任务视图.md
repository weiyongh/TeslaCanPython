# 当前总部任务视图

**确认日期：** 2026-09-14  
**审核状态：** `APPROVED / CURRENT VIEW`  
**性质：** 当前任务优先级与依赖关系的轻量视图，不替代各项目合同、方法规范或具体任务书。

## 任务1：伯恩 HVAC Round 4

今天完成“全关 → 仅送风 → 制冷 → 仅送风 → 全关”实车数据采集。采集回来后立即交伯恩继续 Blind Reverse，观察通风与制冷能否解耦以及能够形成哪些 Evidence。

## 任务2：罗格实车最小闭环

点烟器适配器和线到齐后，今天完成：

```text
USBcan-II → can0 → 500 kbps / listen-only → 实车采集 → 停止 → 日志读取
```

本轮只验收最小闭环，不扩展罗格功能。

## 任务4：Model 3 Baseline Coverage Map

本任务是总部下一阶段主任务。建立一张轻量的 `Model 3 Baseline Coverage Map`，先回答：

- 我们需要哪些诊断基线；
- 已经覆盖到哪里；
- 哪些只是候选、过程证据或历史材料；
- 真正缺少哪些 Evidence。

当前不立即扩展成大量 Case、采集计划或自动化系统。今天先完成任务1、2，不启动本任务的大规模工作。

## 任务3：历史 ASC 浅层资产盘点

本任务服从任务4。历史 ASC 先只做浅层资产盘点，不逐个进行完整质量审计，也不预先重跑全部分析。

后续由 Baseline Coverage Map 提出具体 Evidence 缺口，再定向调用和深挖相应历史 ASC：

```text
任务4：我们需要什么 Baseline
            ↓
任务3：历史 ASC 中已经有什么可以满足它
            ↓
       仍然缺失的 Evidence
            ↓
       最小必要补采计划
```

统一采用“按需求找数据”，不采用“先把所有旧数据重新分析一遍”。

## 任务5：Windows迁移前的最小资产保全

本任务服务于任务6，只保护“不可重建且当前需要”的有效资产，不整理全部工程、不统一Git状态，也不完整复制Mac环境。实施范围和验收要求见[任务5实施备忘](任务5_Windows迁移前最小资产保全清单.md)。今天不执行，先完成任务1、2。

## Baseline Evidence 准入原则

采集目的不决定最终资产身份：

- 伯恩为 Blind Reverse 采集的数据，如果实验条件、独立 Event Evidence、状态解耦和审核结果足够好，可以晋升为总部 Baseline Evidence；
- 名义上的“基线采集”如果证据质量不足，也不能进入正式基线。

是否准入由实际 Evidence 质量和审核结果决定，不由文件名、原始任务名称或采集者决定。

## 额外任务6：Windows出差工作环境准备（非项目任务）

用户出差期间只能使用富士 `LIFEBOOK U939X/B` 笔记本和Windows环境。出发前需要建立并实际验证一套可工作的Windows环境，确保离开macOS后仍能与Pi及各项目协同，不因操作系统、代码、凭据、工具或数据不可用而中断工作。

最低验收结果：

- Windows笔记本能够正常使用Codex及所需账号；
- 能够访问当前需要工作的总部、伯恩、叶问、罗格和班吉项目资料；
- Git、Python及当前实际需要的分析命令能够运行；
- 能够读取、检查和传递ASC、DBC、Markdown、JSON、CSV、图片及采集包；
- 需要连接车辆或采集设备时，相关驱动、接口和只听安全边界已单独验证；
- 重要凭据和数据采用安全方式迁移，不把密码或Token写入项目文件；
- 在离开macOS前至少完成一次Windows端到端试运行，并保留必要的离线恢复资料。

本任务独立于TeslaCanPython项目开发，不借此扩展或重构各项目；具体实施方案在导演明确启动后单独制定。
