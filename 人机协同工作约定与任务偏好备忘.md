# TeslaCanPython 人机协同工作约定与任务偏好备忘

**文档性质：** 工作备忘  
**形成日期：** 2026-09-10  
**审核状态：** 已审核通过  

## 1. 适用范围

本文记录用户与 AI 在 TeslaCanPython 项目中的协同方式、任务推进习惯和完成偏好，供后续任务参考。

CANVoiceRunner `doc/V2优化.md` 中除 Java 语言和 App 特定实现要求以外的协作约定，原则上适用于本项目。若本文、项目当前规范与用户最新指令发生冲突，以用户最新指令为准。

## 2. 基本协作方式

1. 先理解问题、读取当前有效资料并提取任务，不直接跳入设计或实现。
2. 讨论中逐项收敛目标、范围、术语、边界和验收要求；未经确认，不自行扩展需求。
3. 用户的最新决定覆盖较早的假设或设计。发生变化时应显式修正文档，不保留相互冲突的当前结论。
4. 严格区分讨论、设计、实现、测试和完成；状态按实际进展更新，不以计划或局部成果冒充下一阶段。
5. 用户要求“先讨论”“等待审核”或“等待下一步指令”时，不提前修改程序、模板或正式产物。
6. 在已授权的实现范围内，普通、可逆的代码修改无需逐处请求确认，但完成后必须执行与风险相称的验证。
7. 方案服务当前单车、单人、人工审核工作流；不为未发生的多用户、多车辆、多项目场景预建抽象层。
8. 用户说“审核通过”时，只表示当前提交项已被接受并完成，不构成进入设计、实现、测试或其他后续工作的授权；无需用户再补充“等待下一步指令”。

## 3. 文档偏好

1. 人类可读任务文档、讨论结论、设计、测试记录和审核记录优先使用 Markdown（`.md`）。
2. 机器接口文件保留其适当格式，例如采集播报脚本使用 `.txt`，时间线使用 `.csv`，结构化元数据和 Schema 使用 `.json`。
3. 文档应简洁、结构明确，重点说明问题、原因、决定、边界、验证方法和未完成事项。
4. 仅沉淀用户明确要求记录的内容，以及准确表达这些内容所需的最小上下文。
5. 草案、待审核、已审核、已验证和未完整验证必须明确区分，不能把讨论内容写成既定事实。
6. 人类可读文档默认使用中文。复杂或需精确指认的技术概念采用“中文名（准确英文名）”；代码标识符、字段、文件名、枚举和状态常量保留原始英文。
7. 任务相关文档统一放入 `doc/<任务名称>/`，由任务统一工作文档索引。

## 4. 任务推进与人工审核

默认参考流程为：

```text
任务提取
→ 讨论
→ 设计
→ 设计审核
→ 实现
→ 代码/产物审核
→ 测试或验证
→ 测试审核
→ 问题修正与回归验证
→ 完成确认
→ 更新任务文档
→ Git提交（适用时）
→ 任务关闭
```

任务可以按用户指令省略、合并或延后某些阶段，但 AI 不得自行跨越人工审核门。

统一工作文档应至少记录：

- 任务名称、目标和范围；
- 开始时间、结束时间和当前状态；
- 用户提供的原始要求；
- 已确认决定与待确认问题；
- 设计、验证和其他关联文档；
- 已验证与尚未完整验证内容；
- 最终完成范围和适用边界。

## 5. 设计与实现偏好

1. 先建立准确语义，再选择实现方式；不得用实现方便性改写业务概念。
2. 优先采用满足当前需要的最小方案，不主动引入复杂架构、大型依赖、数据库、通用工作流引擎或自动版本系统。
3. 抽象设计宜先通过真实 Case、最小示例、Mock 或简单示意验证，真实需求暴露缺口后再扩展。
4. 程序、文档和机器接口中的身份、时间、状态、空值及对象关系必须含义明确、可审计。
5. 已有用户文件和历史产物保持原状；没有明确迁移要求时，不批量重命名、移动或重建。
6. 代码实现完成后必须执行与变更风险匹配的单元测试、静态检查、构建或实物验证。无法执行或覆盖不足时必须如实报告。

## 6. 任务完成偏好

1. 文档写入、代码修改或部分测试通过，均不自动表示任务完成。
2. “完成”要求目标实际达到、必要验证通过、人工审核完成、工作文档收尾，并在适用时完成 Git 提交。
3. 未验证项必须单列，说明是否影响本次完成以及后续如何处理。
4. 对证据不足、平台限制或实现边界应直接说明，不以表面可用或宽泛免责声明代替结论。
5. 最终交付应简洁明确，说明完成了什么、验证了什么、仍有什么边界以及下一步是否需要用户决定。

## 7. 本次审核

本文根据当前对话和 CANVoiceRunner 既有协作约定整理，并于2026-09-10经用户人工审核通过。
