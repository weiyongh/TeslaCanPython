# 计划时间与实测事件时间分离测试

## 测试状态

自动化测试和目标手机核心流程测试已通过。2026-09-09 用户确认任务 3 当前范围已确认通过；锁屏、人工听感和同次 ASC 对齐作为已记录的后续现场验证边界保留。

## 自动化测试环境

- 日期：2026-09-09
- Java：OpenJDK 17
- Gradle：项目 Wrapper
- 构建变体：`debug`

## 已执行的自动化验证

```text
./gradlew testDebugUnitTest assembleDebug lintDebug
```

结果：`BUILD SUCCESSFUL`。

### 单元测试

共 11 项，0 失败，0 错误，0 跳过：

1. `SessionTimebaseTest`：3 项，验证暂停期间 `script_time` 持续、调度时间冻结、恢复续接以及多次暂停累计。
2. `EventRecordTest`：4 项，验证 `pending / triggered / skipped` 转换、触发前跳过、触发后纠正与首次审计时间不被覆盖。
3. `SessionCsvExporterTest`：1 项，验证 `start_clock`、`end_clock`、原始 `plan_time`、`trigger_script_time_us`、`skip_script_time_us` 和 CSV 转义。
4. `SessionTimelineRunnerTest`：3 项回归通过，验证既有调度、跳过过时节点和完成回调。

### 构建与静态检查

- `assembleDebug`：通过，生成 `app/build/outputs/apk/debug/app-debug.apk`。
- `lintDebug`：通过，0 个 Error、10 个 Warning。
- 10 个 Warning 均不是任务 3 运行逻辑错误：包括 SDK/Gradle 版本提示、竖屏限定、应用图标以及中文硬编码界面文本的国际化提示。
- `git diff --check`：通过，未发现空白符错误。

## 实现审查中发现并修正的边界

暂停播报时不允许导入新脚本。否则界面会显示新脚本，后台服务却仍执行原脚本，导致采集语义错位。该边界已修正，并已重新执行全部自动化验证。

## 目标手机核心流程测试

- 设备：一加 Ace 5（ADB 型号 `PKG110`）
- 安装包：`com.teslacan.voicerunner.v2debug`
- 方式：Debug APK 覆盖安装，使用内置 12 节点脚本真机执行

已确认：

1. Clock 按 `HH:mm:ss` 正常显示。
2. 暂停期间中央采集时间从 `02:28` 增长到 `03:29`，下一操作仍显示原始 `02:04 结束采集`。
3. 触发后点击跳过，界面恢复后显示 `skipped`；自然结束 CSV 中 `E11` 保留 `trigger_script_time_us=109003228` 并记录 `skip_script_time_us=209884522`。
4. 暂停恢复后，原计划时间 `124 s` 的 `E12` 在连续 `script_time` 的 `244049590 us` 触发；原 `plan_time_s` 未改写。
5. 自然结束的 `session_script_time_us=244082630`，前台服务正常退出前台状态。
6. 人工结束弹出导出确认框；导出 CSV 包含完整 `start_clock`、`end_clock`、原始 `action`、`plan_time_s` 和整数微秒字段。
7. 返回桌面等待 12 秒再进入 App，界面恢复为 `00:14`，事件仍为 `triggered`，证明运行态前后台切换未重置会话时间或事件状态。

真机截图：

- [暂停期间 script_time 继续](任务3/任务3_真机暂停.png)
- [自然结束](任务3/任务3_真机自然结束.png)
- [导出确认](任务3/任务3_真机导出确认.png)
- [后台返回](任务3/任务3_真机后台返回.png)

## 仍需现场验收的项目

以下项目需要人工感知、锁屏或真实采集数据，不能由本地 JVM 单元测试或自动点击替代：

1. 人工确认暂停期间 TTS 确实停止，恢复后语音内容、时机和听感正确。
2. 事件触发前在真机点击“跳过”，确认该事件不播报，导出为 `skipped`，且触发时间为空。
3. 在暂停态退到后台并返回，以及运行态、暂停态分别锁屏后返回，确认 `script_time`、暂停状态、事件状态与 Clock 锚点不重置。
4. 将 App 记录与同次 ASC 的时间域进行对照；若 ASC 不使用同一绝对时钟，必须额外保留可审计的映射依据，不得只用 Clock 格式相同作为已对齐证明。

## 当前结论

任务 3 的时间模型、事件状态和导出结构已经通过本地自动化测试、Debug 构建和目标手机核心流程测试。2026-09-09 用户确认本任务阶段完成。

尚未完整验证的项目是锁屏、暂停状态下的后台切换、人工 TTS 听感、真机触发前跳过，以及同次 ASC 时间对齐。其中时间算法和触发前跳过已有单元测试覆盖；其余项目属于现场、ColorOS 或后续 ASC 联合验证边界，不阻塞任务 3 按当前范围完成。最新审核的中央计时、顶部“耗时”和按钮视觉置灰调整，按用户要求延后到后续 UI 相关任务实现，不影响本任务数据与时间逻辑的完成。
