# 后台计时与语音播报优化测试

## 测试结论

任务 2 的自动化测试、构建检查和一加 Ace 5 首轮真机验证通过。已发现的问题完成修正并通过回归检查，可以在保留已注明测试边界的前提下结束本任务。

## 自动化验证

- `testDebugUnitTest`：通过，3 个调度单元测试无失败；
- `assembleDebug`：通过，Debug APK 构建成功；
- `lintDebug`：通过，无阻断错误；
- `git diff --check`：通过。

调度测试覆盖计划事件点单次触发、已过去节点不补播、后续节点继续遵循原时间轴，以及完成后不再播报。

## 一加 Ace 5 真机验证

- V2 Debug 包安装和冷启动成功；
- 前台服务状态为 `isForeground=true`；
- App 切换后台后服务与计时持续运行；
- 常驻通知存在并显示计时及下一操作；
- 返回 App 后计时未重置，当前步骤和下一步骤正确；
- 未发现应用崩溃；
- ColorOS 对高频通知更新进行限流的问题已修正，回归测试未再出现限流日志；
- 测试结束后前台常驻状态正常停止。

## Review 修正

- 恢复用户手动结束采集后的 CSV 导出询问；
- 采集运行期间禁止导入新脚本，避免界面脚本与后台运行脚本不一致；
- 通知权限改为授权完成后再启动采集；拒绝授权时给出明确提示和设置入口；
- Debug 构建使用独立包名，可与手机中的旧版并存。

## 保留边界

以下场景尚未形成完整验证记录，但不阻止本次任务完成：

- 锁屏后的完整脚本播报；
- 从最近任务中移除 App 后的 ColorOS 行为；
- ColorOS 不同电池优化设置下的长时间运行；
- 完整运行至示例脚本自然结束后的 CSV 导出；
- 每个语音节点的清晰度和听感仍以用户实际使用确认为准。
