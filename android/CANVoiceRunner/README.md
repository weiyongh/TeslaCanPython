# CAN 采集播报器

面向车辆 CAN 实验的离线 Android 脚本执行器。它读取仓库现有的 `00s  动作` 文本格式，提前 5 秒播报动作，在最后 3 秒发出倒计时音，并在计划时刻发出清晰执行音。

第一版功能：导入 UTF-8 文本脚本、大字显示当前/下一步骤、开始/暂停、上一步/下一步、Android 系统中文 TTS、保持屏幕常亮、记录实际触发时间并导出 CSV。

用 Android Studio 打开本目录。首次同步完成后选择真机或模拟器运行。建议在手机“文字转语音输出”设置中预先下载中文离线语音。
