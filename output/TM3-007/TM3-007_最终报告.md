# TM3-007 开锁开门完整上电：车型条件化基线分析

## 实验信息与适用范围

- 用途：正常基线数据采集。
- 车辆：Tesla Model 3 上海产2021款 标准续航 55 kWh 后驱。
- 完成状态：COMPLETE_WITH_GAPS；范围：THIS_EXPERIMENT_ONLY。

## 事实与关键证据

- 600秒ASC格式完整且525946帧均可解析
- 103.1571–470.1237秒为本采集域无帧窗口
- 470.1237秒后通信恢复并形成预充/接触器/Pack电压/DCDC上电序列
- 门闩CAN候选形成CLOSED→OPENED→CLOSED
- 挡位和电驱状态形成P/STANDBY→D/ENABLE→P/STANDBY

## 控制关系

- **系统进入/状态推进主线**：本采集域低通信候选 → 通信恢复 → 高压建立 → D/ENABLE → 返回P/STANDBY稳定  
  证据：`报文频率(派生)`（`derived`）；`活跃CAN_ID数(派生)`（`derived`）；`DI_gear`（`0x118`）；`DI_systemState`（`0x118`）  
  这是实测CAN状态推进，不等同于已经证明整车真实唤醒或ECU内部完整控制算法。
- **高压控制链**：HVIL候选转为STATUS_OK → 正接触器候选进入PRECHARGE → BMS总接触器CLOSING/CLOSED → 正负接触器候选进入ECONOMIZED  
  证据：`HVP_hvilStatus`（`0x20A`）；`HVP_packContPositiveState`（`0x20A`）；`BMS_contactorState`（`0x212`）；`HVP_packContNegativeState`（`0x20A`）
- **高压物理响应**：Pack端电压约5 V → 约354 V  
  证据：`BMS_packVoltage`（`0x132`）  
  BMS_packVoltage是Pack端反馈，不是接触器下游DC Link电压；当前Approved范围没有独立DC Link电压证据。
- **低压能源响应/交叉验证**：DCDC支持IDLE → ACTIVE；PCS侧低压电压/电流候选同步建立  
  证据：`PCS_dcdc12VSupportStatus`（`0x224`）；`PCS_dcdcLvBusVolt`（`0x2B4`）；`PCS_dcdcLvOutputCurrent`（`0x2B4`）  
  无外部12V仪表实测，0x2B4定义差异继续保留。
- **可驱动状态及结果反馈**：UI Ready候选0 → 1；其后挡位P → D且电驱STANDBY → ENABLE，最终返回P/STANDBY  
  证据：`UI_readyForDrive`（`0x353`）；`DI_gear`（`0x118`）；`DI_systemState`（`0x118`）  
  UI_readyForDrive仅作显示/消费层交叉反馈，不解释为驱动许可源头。

### 主线核验摘要

| 主线节点 | Approved Signal | 当前验证程度 | 局部限制 |
| --- | --- | --- | --- |
| 本采集域通信恢复 | `报文频率(派生)`（`derived`）；`活跃CAN_ID数(派生)`（`derived`） | 采集域内通信变化PARTIALLY_VALIDATED。 | 不确认整车真实休眠/唤醒，不反推人工解锁时刻。 |
| 高压安全条件候选满足 | `HVP_hvilStatus`（`0x20A`） | 本实验既有结果STRONGLY_SUPPORTED。 | 0x20A多DBC并列验证；无独立HVIL外部观测。 |
| 预充/接触器执行 | `BMS_contactorState`（`0x212`）；`HVP_packContPositiveState`（`0x20A`）；`HVP_packContNegativeState`（`0x20A`） | 本实验既有结果STRONGLY_SUPPORTED。 | 0x20A结果不升级为车型级正式语义。 |
| Pack高压建立 | `BMS_packVoltage`（`0x132`） | Pack侧物理响应STRONGLY_SUPPORTED。 | 不等同于所有下游高压母线均已独立验证。 |
| DCDC低压支持 | `PCS_dcdc12VSupportStatus`（`0x224`）；`PCS_dcdcLvBusVolt`（`0x2B4`）；`PCS_dcdcLvOutputCurrent`（`0x2B4`） | 支持状态与低压电压STRONGLY_SUPPORTED；输出电流PARTIALLY_VALIDATED。 | 无外部12V测量；0x2B4存在定义差异。 |
| 可驱动状态建立 | `DI_gear`（`0x118`）；`DI_systemState`（`0x118`）；`UI_readyForDrive`（`0x353`） | 挡位/电驱状态及显示层交叉反馈均为STRONGLY_SUPPORTED。 | UI_readyForDrive只作显示/消费层交叉反馈，不作为控制许可源头。 |
| 返回P/STANDBY稳定状态 | `DI_gear`（`0x118`）；`DI_systemState`（`0x118`） | CAN内部返回状态STRONGLY_SUPPORTED。 | 不反推驾驶员动作精确时刻。 |

## 基线结论

- 建立了本采集域低通信候选到高压建立、DCDC支持及电驱D/ENABLE的条件化CAN内部上电基线。
- 本实验完成状态为COMPLETE_WITH_GAPS。

### Evidence Assessment

- `ER-01`：`SUPPORTED` — 103.1571–470.1237秒无CAN帧，随后通信恢复并进入约2200–3000帧/秒；边界：支持本采集域低通信候选→网络唤醒；不升级为整车真实休眠。
- `ER-02`：`INSUFFICIENT_EVIDENCE` — 0x102在500.7386秒开、525.7703秒关；UI_lockRequest只见IDLE；无独立动作锚点；边界：门闩反馈可区分，但解锁请求和人工开门时刻不能独立确认。
- `ER-03`：`INSUFFICIENT_EVIDENCE` — 门闩关闭可见；0x30A无帧；边界：占座节点无直接观测，不能区分入座与关门各自贡献。
- `ER-04`：`INSUFFICIENT_EVIDENCE` — 0x118在540.3854秒后可见，但DI_brakePedalState全程INVALID；边界：无法直接确认制动输入；仅能评价其后挡位/电驱内部状态。
- `ER-05`：`SUPPORTED` — HVP_hvilStatus由UNKNOWN转STATUS_OK并与0x682 OK交叉；发生在预充和接触器闭合前；边界：至少一个高压安全条件候选形成状态门时序；绝缘Signal仍验证失败。
- `ER-06`：`SUPPORTED` — BMS总接触器CLOSING→CLOSED；正接触器PRECHARGE→ECONOMIZED；负接触器OPEN→ECONOMIZED；Pack电压约5V升至约354V；边界：状态序列和物理电压响应形成实验级闭环。
- `ER-07`：`SUPPORTED` — 545.2670秒DI_gear P→D且DI_systemState STANDBY→ENABLE；UI ready候选此前已为1；边界：挡位反馈和电驱ENABLE满足内部可驱动状态证据；缺少独立仪表/动作时刻。
- `ER-08`：`SUPPORTED` — DCDC支持IDLE→ACTIVE；ETH/JSON低压候选约12.44V升至稳定13.48–13.51V，电流由0升至约24–33A；边界：DCDC状态与低压能源响应方向、时序和量级闭合；无外部12V实测。
- `ER-09`：`SUPPORTED` — 555.4262秒D→P且ENABLE→STANDBY；其后接触器CLOSED、DCDC ACTIVE及低压电压稳定至600秒；边界：形成约44.6秒P挡上电稳定窗口。

## 证据边界

- 独立Observed Event Time缺失；Planned Time只能用于粗定位。
- 不把CAN变化反推为人工动作时间，不形成精确“人工动作→CAN响应”延迟。
- 103.1571–470.1237秒只称为本采集域低通信候选，不称为整车真实休眠。
- 结论限定为既有证据支持的CAN内部状态转换、顺序和条件化状态链。
- 制动字段INVALID、占座报文无帧；解锁请求和外部12V实测缺失。
- 0x20A仅迁移本实验既有Signal Validation结果，不升级车型级语义。

## 结论与下一步建议

- 无需整体重采；最小补采仅补独立Observed Event Time、占座/制动证据及外部12V电压，复核0x2B4低压定义。
- 当前不进入车辆故障诊断树。
