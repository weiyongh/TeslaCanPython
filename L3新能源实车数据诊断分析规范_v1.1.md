# L3新能源实车数据诊断分析规范 v1.1

**版本：** v1.1  
**日期：** 2026-09-01  
**状态：** 当前有效  
**前版：** `L3新能源实车数据诊断分析规范_v1.0.md`（保留，不覆盖）

## 1. 总纲与阶段定位

本规范继承v1.0的系统分层、四条主线、控制树、状态机、基线/故障用途区分和四层报告逻辑，并固化TM3-009/010公共报告链路回归形成的方法。

> 系统理解是基础，通信Signal是路径，逻辑推理是方法，控制树是目标。

> 数据服务于控制关系，控制关系服务于诊断。

当前阶段采用**实验驱动，而不是知识库驱动**。Model 3控制树、状态机、Signal语义和充分性规则仍会由实车实验修正；单次审核结果不得自动提升为车型永久知识。

工作循环：`L3当前认知 → 实验目的/设计 → 待审Evidence Plan → 人工审核 → 实车采集 → 数据分析 → Evidence Assessment → 基线结论/诊断判断 → 修正认知 → 下一实验`。

## 2. 系统边界与目标区分

每次实验先明确系统/子系统、边界内对象、边界外输入和边界外输出。系统边界决定某变量是控制节点、外部条件还是物理结果。

必须区分：

- **实验目标/驾驶员目标**：实验者希望达到的工况，例如约20 km/h。
- **控制器内部目标**：ECU算法实际建立并控制的目标量，只有直接定义或充分证据才能确认。
- **物理结果**：车辆实际速度、加速度、温度、声音等。

外部目标不能因名称相似自动写成ECU内部目标。无巡航的稳定匀速实验中，目标速度属于驾驶员—车辆实验闭环；车辆电驱边界内观察到的是外部驾驶输入、状态/条件/能力、仲裁、扭矩目标、执行反馈和车辆运动。

## 3. 主验证线与交叉验证线

四条主线仍为动力链、能源链、控制链和安全链。单次实验应明确：

- **主验证线**：直接回答实验命题的控制关系。
- **交叉验证线**：从不同观察层验证主线方向或状态一致性的独立证据。

对驱动实验，推荐分开表达：

`控制链：驾驶输入 → 状态/条件/能力判断 → 仲裁/决策 → 仲裁后扭矩请求 → 执行反馈 → 车辆运动`

`动力/物理响应：实际扭矩 → 轴速/车速`

`能源交叉验证：电驱功率 ↔ Pack电流/电压/同帧V×I功率`

禁止写成“电门→扭矩→车速→电驱功率→电池”的单一串行因果链。

## 4. 控制关系与Signal证据投射

控制关系首先来自当前L3控制树片段，Signal只是其车型实车证据投射。不得根据本次可读Signal列表反向定义或压缩控制树。

控制节点的观测状态至少可表达：

- 直接观测；
- 部分可观测；
- 本次无直接观测；
- 由上下游证据间接支持。

缺少直接Signal时保留节点并注明观测边界。Signal不能自动等同内部算法、轮端力、液压压力或其他物理量。

`Control Relationship View`由实验分析/Evidence Plan上游形成，至少包含控制链节点及观测状态、动力/物理响应、能源交叉验证和关键边界。Renderer只能展示该View，不得从Approved Signal列表生成控制关系。

## 5. Evidence Requirement与动态Signal角色

从实验目的和控制树片段逐节点提出Evidence Requirement：需要证明什么状态、请求、执行、反馈、物理结果或能源对应关系；再从车型DBC、物理测量和实测数据中寻找候选证据。

Signal的物理意义、DBC来源、单位和已知限制可以相对稳定；但下列属性必须按当前实验动态确定：

- 本实验诊断角色；
- 主验证/状态门/能力背景/能源交叉验证/专项/审计角色；
- P0/P1/P2/P3；
- 报告位置和排序；
- 充分性规则。

不得把单次实验优先级写成Signal永久属性。

## 6. Draft、Review与Approved机制

公共链路：

`Draft Evidence Plan → Human Review/Override → Approved Evidence Plan → Evidence Assessment → Report View Model → Renderer`

Draft至少记录Signal、本实验角色、建议优先级、建议报告位置、推导理由、语义状态、置信度、不确定标记和是否需审核。

下列情况优先要求人工审核：节点绑定不确定、候选语义、DBC冲突、多角色、主次接近、报告位置争议、系统边界附近、跨主线证据、直接/代理关系不清。

人工可接受、覆盖或排除建议。Approved必须同时保留原始建议与effective结果，范围固定为`THIS_EXPERIMENT_ONLY`。未审核的不确定项不得进入Approved，审核结果不得自动写回车型知识。

### 6.1 实车采集前三文件与证据来源设计

每个TM3实验在采集前生成：`TM3-xxx_实验采集方案.md`、`TM3-xxx_语音执行脚本.txt`、`TM3-xxx_现场采集记录.md`。具体接口和模板见`doc/methodology/实车采集前文档体系与分析接口.md`与`doc/templates/experiment_collection/`。

实验方案必须按`实验问题 → 车内CAN → SMT/诊断工具 → 人工事件 → 外部物理证据`设计来源。结论跨越车辆边界时必须主动寻找边界外证据。Signal Validation成熟度不足或DBC冲突应在采集前转化为独立证据要求、现场记录项和结束前Checklist。

语音TXT只负责计划动作与必要口述/拍摄提示。现场记录保存实际条件、Observed Event、外部证据和偏差。`Planned Time / Observed Event Time / CAN Observed Time`不得互相替代；分析可研究Observed到CAN的响应，但不得用CAN反推Observed后形成循环证据。

实验完成状态为`COMPLETE / COMPLETE_WITH_GAPS / PARTIAL / INVALID / ABORTED`，由Evidence Requirements满足度决定。Checklist须在人离开现场前发现ASC、实际事件锚点、SMT、外部读数、照片/视频、订单/累计量和偏差记录缺口。

## 7. Evidence Assessment

程序状态至少包括：

- `SUPPORTED`：有效证据支持Requirement；
- `CONTRADICTED`：有效证据与预期明确矛盾；
- `INSUFFICIENT_EVIDENCE`：有相关证据但不足以判定；
- `NOT_OBSERVED`：本次数据未观察到所需状态/事件；
- `NOT_APPLICABLE`：当前状态或实验范围不适用。

人读语言结合多个Requirement、状态覆盖和替代解释形成“已确认、强支持/本次范围内未发现异常、待验证、与预期不符”。“原因已确认”必须形成根因闭环，不能由单个SUPPORTED自动产生。

### 7.1 第三方DBC Signal Validation

关键Signal出现物理不合理、控制链矛盾、实际动作不符、Request/Available/Actual关系异常、跨DBC定义冲突、DLC/MUX/位段/缩放/符号版本差异、SNA/越界/异常枚举或守恒不闭合时，必须先执行`doc/methodology/第三方DBC_Signal_Validation流程.md`，区分车辆异常与测量/解释链异常，再决定是否进入故障诊断树。

Signal Validation必须回到原始bit、比较全部可用DBC/JSON来源、寻找独立字段、验证完整状态转换、控制关系、物理守恒、累计量和外部证据，并输出独立的Signal成熟度。P0/P1/P2/P3表示当前实验重要度，成熟度表示定义可信度，两者不得互相推导。禁止仅凭单次实验闭合所需factor修改正式DBC。

## 8. Report View Model与Renderer边界

Report View Model由上游提交：实验metadata、事实、Control Relationship View、Assessment、结论与建议、审核后Signal角色/位置/排序、时间线、统计表和审计信息。

Renderer仅负责固定呈现：

- 只接受Approved Evidence Plan；
- 只消费`effective_role / effective_priority / effective_report_position / effective_order`；
- 不根据Signal名称、CAN ID、变化幅度或解码状态重新判断重要度；
- 不裁剪上游已批准事件，不引入未经批准Signal；
- 控制链、动力/物理响应和能源交叉验证分别展示；
- 人读数值按诊断意义格式化，不泄漏二进制浮点尾数；原始精度保留在CSV/审计；
- 模板拼接不得产生双重标点。

DBC关键Signal表的“本次用途”必须是Signal级具体诊断作用，由Approved Plan/Report View Model提供，不能整表复制实验总目的。

## 9. 基线有效性与补采决策

报告必须明确：实验是否完成；哪些片段可形成条件化基线；哪些仅为过程证据；是否重采；整体重采还是局部补采；最小必要采集；是否进入诊断树。

当一个子工况失败而其他子工况合格时，不重复已合格部分。稳定窗口以实际状态/测量量进入并稳定于判据为起点，脚本预定时间只用于定位。补采建议应说明最小持续时间及SOC、温度、坡度/方向、空调等主要条件。

## 10. Golden Regression Samples

TM3-009和TM3-010是Golden Regression Samples，保护已验收结论、窗口、关键数值、Signal语义、控制关系边界、人读骨架、审核机制和Renderer职责。

TM3-015能源链DBC适配专项是Signal Validation首个Golden Case，保护`0x132`多路定量闭合、`0x29D`仅时序可信、`BMS_chgPowerAvailable`优先bit40但仍为能力候选的结论边界。详细流程与案例见`doc/methodology/第三方DBC_Signal_Validation流程.md`。

未来重构若产生差异，应先报告`Regression Failure`。只有原始数据或算法真实错误得到可复现证明后，才可通过人工审批修订Golden结果。

## 11. 当前暂停范围

当前不建设正式L3知识库、全量车型Signal Semantic Mapping数据库、自动Evidence Plan编译/评分、自动控制树生成、自动故障原因确认、审核GUI或Notion规则自动同步。知识成熟后再知识化，不以默认优先级掩盖缺口。

## 12. 新任务进入顺序

1. 读取根目录`AGENTS.md`和本规范；
2. 明确用途、实验目的、系统边界、主线、控制树片段和状态机；
3. 采集前生成实验方案、语音脚本和现场记录模板，将Evidence Gap和Signal Validation缺口前移为独立证据与Checklist；
4. 采集后读取现场记录、实际事件锚点、外部证据、ASC/DBC、已有Golden测试和相关历史结论；
5. 生成待审Evidence Plan并完成当前实验审核；
6. 分析、Assessment、结论与最小下一步；
7. 使用公共Renderer输出并运行Golden Regression。

输出原则：**后台分析可以复杂，前台诊断必须简单；证据可以很多，结论必须明确。**
