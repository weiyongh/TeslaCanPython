# L3新能源实车数据诊断分析规范 v1.0

**版本：** v1.0\
**日期：** 2026-08-31\
**定位：** 新能源 L3 学习、实车基线采集、CAN/DBC
数据分析、故障案例分析及 Codex
自动分析程序的统一"诊断语言、推理规则与报告协议"。

## 0. 总纲

> **系统理解是基础，通信信号是路径，逻辑推理是方法，控制树是目标。**

实车诊断：先知道系统正常时应该怎样工作，再通过
Signal、状态和物理测量判断实际工作到了哪一步；预期关系不成立时，才进入诊断树/故障树继续定位。

所有分析统一遵循：**事实 → 控制关系 → 诊断判断 → 证据边界**。

输出原则：**后台分析可以复杂，前台诊断必须简单；证据可以很多，结论必须明确。**

**报告表达修订（2026-08-31，用户确认）：** “三个实验问题”仅为TM3-009训练阶段校准分析思路的显式检查项，不作为通用报告的固定章节或固定问题数量。Codex应根据实验目的、控制树片段和状态机自主生成内部诊断问题，核对证据与控制关系并完成自检；最终报告将答案综合为“诊断判断”，按“证据→判断→结论”自然表达。只有实验脚本本身明确提出需要逐项回答的问题时，才保留“问题—回答”形式。四层分析逻辑不要求机械设为正文标题；基线采集中的判断仍服务于控制关系与车型基线结论，不转为车辆故障判定。

## 1. 系统、分层与四条主线

诊断首先回答：功能/故障属于哪个系统、处于什么层级、上下游是什么、输入输出是什么、谁控制、谁执行、谁反馈、需要哪些条件。

统一层级：L3 系统级；L4 子系统/功能模块级；L5 部件/器件级；L6
信号/参数级。不能一开始就陷入 Signal，Signal
必须放回系统和控制关系中理解。

四条主线：

-   **动力链**：扭矩/机械作用如何形成并产生车辆运动。典型：驾驶请求 →
    扭矩目标 → 电驱执行 → 电机 → 减速器 → 车轮 → 车辆运动。
-   **能源链**：能量从哪里来、如何转换、流向哪里。驱动：动力电池 →
    高压母线 → 逆变器 → 电机 → 机械输出；回收方向相反。
-   **控制链**：谁提出需求、谁判断条件、谁形成目标、谁执行、谁反馈。典型：输入
    → 条件确认 → 状态评估 → 能力评估 → 授权/仲裁/决策 → 目标 → 执行 →
    反馈 → 持续监控。
-   **安全链**：功能是否允许，以及异常时如何限制、退出和恢复。安全链通常表现为控制链中的条件、约束、保护和反馈分支。

分层回答"在哪一层"，主线回答"沿哪条作用关系走"。

## 2. 图的分工

不同关系不得强行塞进一张图：系统组成图回答"由什么组成"；系统边界图回答"与谁交互"；能源/高压配电图回答"能源怎样分配和流动"；控制关系图回答"请求、决策、执行、反馈"。过程与条件可用顺序控制图；状态及迁移使用状态机。每张图只表达一种主要关系。

## 3. 控制树

控制树回答：**一个功能在正常情况下，是怎样一步一步被允许、决策、执行并确认结果的？**
控制树不是 ECU 列表，也不是 CAN ID 列表。

统一阶段：功能目标 → 请求/输入 → 条件确认 → 状态评估 → 能力评估 →
授权/仲裁/决策 → 请求/目标 → 执行 → 反馈/确认 → 物理结果 → 持续监控 →
保护/限制/退出/恢复。

具体功能可以裁剪，但不得破坏"请求---条件---决策---执行---反馈"的基本逻辑。**主线告诉我们沿哪里走；控制树告诉我们每一步为什么能够继续走。**

## 4. 状态与状态机

状态是控制逻辑成立的前提。同一个 Signal
在不同状态下可能有完全不同的诊断含义。分析动态数据前必须先问：**车辆现在处于什么状态？**

状态机回答系统有哪些状态，以及什么条件触发状态迁移。状态机节点必须是状态，不把
BMS、MCU、CAN 或故障源画成运行状态。

**状态切换也是数据分析边界。** 正驱动 → 回收 →
脚刹属于不同控制工况。跨状态窗口必须识别、分段、分别解释，禁止用总体均值掩盖状态差异。

## 5. Signal / CAN 的定位

CAN 和 Signal
是**观察控制关系和状态变化的诊断路径，而不是诊断终点**。它们可以帮助证明请求、状态、允许条件、目标、执行反馈、能源输出和时间对应关系，但不能自动等同于实际端电压、机械力、液压压力、声学结果或真实内部算法。

必要时继续下钻：**Signal → 控制器输出 → 供电/线束/连接 → 执行器 →
物理结果**。

## 6. 基线与车型实例化

正常实车采集不是为了收集大量 CAN
数据，而是**给控制树中的关键控制关系建立车型实例化的正常证据**。

例如：驾驶输入 → 扭矩请求 → 扭矩执行 → 车辆运动 →
能源响应。车型实例化后应落实到具体 CAN ID、Signal、DBC
定义、正常时序、变化方向、状态组合、可重复工况和证据边界。

> **基线 = 控制树在具体车型上的实车证据。**

故障样本必须与正常基线分开保存，并用于比较。

## 7. 故障树与诊断树

**故障树**回答：如果预期结果没有出现，可能有哪些原因？它组织候选原因及逻辑组合，不能因为一个异常
Signal 就直接宣布零件故障。

**诊断树**回答：面对当前故障现象，下一步应该验证哪个节点，怎样逐步缩小范围？

> **控制树描述正常应该怎样走；诊断树寻找实际在哪一步没有按预期走。**

例如"请求存在 →
实际扭矩持续不跟随"，诊断入口就在"请求之后、执行/反馈之前"，而不是重新从所有系统无差别搜索。

统一关系：**主线 → 控制树 → 状态机 → 实车 Signal/物理证据 →
符合则形成基线；不符合则进入诊断树 → 用故障树组织原因 → 继续采集/测量 →
缩小范围 → 诊断结论。**

## 8. 实车数据分析标准流程

1.  **先读实验目的。** 禁止先把所有 Signal
    统计一遍，再试图寻找结论。每次实验最好明确不超过三个核心诊断问题。
2.  **明确验证的控制树片段。** 例如：电门 → 请求扭矩 → 实际扭矩 →
    轴速/车速 → 电驱功率 → Pack 放电。统计和图表必须服务于这段关系。
3.  **重建实际状态和动作时间轴。**
    脚本时间只是计划，实车实际动作优先于脚本时间。
4.  **分清六个层级。** 输入；请求/目标；执行；反馈；物理结果；能源响应。

## 9. 输入与最终请求不得混为一谈

-   Accelerator Pedal：驾驶员电门输入。
-   Brake Input：驾驶员制动输入。
-   Torque Request：经过状态、能力、约束和仲裁后的电驱扭矩请求。
-   Torque Actual：电驱实际扭矩反馈。

禁止简单写成"踏板请求 → 请求扭矩"。更准确的是：**驾驶员输入 →
状态/条件/能力 → 仲裁/控制 → 扭矩请求 →
实际执行**。特别在制动、回收、蠕行、TCS/ESP 等工况下必须保持这一分层。

## 10. 异常归因顺序

发现两次数据不同，不得直接进入故障解释。优先检查：驾驶操作 → 起始状态 →
工况/环境 → 数据采集/时间对齐 → DBC/Signal 语义 → 控制系统持续性不跟随 →
物理执行异常。

原则：**能由更简单且已有证据支持的工况差异解释时，不提前调用故障假设。**
但不能为了谨慎拒绝判断。证据支持时，应明确写"本次采集范围内未发现......异常"，而不是只写"没有厂家阈值，因此无法判断"。

## 11. 瞬态与稳态

平均值、MAE、最大差值只是统计量。在状态切换、制动介入、蠕行、回收切换、请求快速变化等瞬态窗口，应优先分析：请求何时变化；实际何时变化；差值持续多久；是持续偏差还是边沿瞬态；稳定后是否重新收敛。

> **差值大小不是唯一核心，差值的时间结构同样重要。**

不同采样周期 Signal
必须说明对齐方法。采样可见延迟不得直接解释为内部真实控制延迟。

## 12. 证据等级与结论语言

-   **A 已确认**：数据或现场测量直接支持。
-   **B 强支持 / 本次范围内未发现异常**：多个变量沿预期控制链一致。
-   **C 待验证**：证据不足或存在解释歧义。
-   **D 与预期不符**：明确观察到控制关系不成立，但根因尚未确认。
-   **E 原因已确认**：证据形成根因闭环。

必须区分：**未观察到 / 观察到与预期不符 /
原因已确认**。正常采集的"未观察到异常"不得外推成"车辆不存在任何故障"。

## 13. Codex 报告分层

### 第一层：诊断正文（给人看）

必须放在报告最前面：

1.  **实验目的**：1～3 句话说明验证哪一段控制树。
2.  **诊断判断**：围绕本次目的，将内部诊断问题的答案综合为连贯判断，按“证据→判断→结论”自然表达，第一屏即可理解发生了什么、是否符合工况预期及原因。不固定问题数量，不单列通用问答章节；仅当实验脚本明确要求逐项回答时采用“问题—回答”。
3.  **关键控制关系**：区分驾驶输入、状态/条件/能力/仲裁、扭矩请求、实际扭矩与车辆运动；动力响应与能源响应分别呈现、相互印证，不强行串成单一时间因果链。证据可融入诊断判断，不必重复设章。
4.  **特殊/异常工况**：单独列出，不与主工况混合统计和下结论。

### 第二层：基线证据附件（面向车型积累）

实车采集增加`采集时间线与关键Signal.md`和`DBC关键Signal覆盖与可读性.md`，保留实际事件、关键节点物理值、变量对应关系及Signal可读性。格式见`doc/methodology/车型基线证据层输出格式.md`，不扩充简洁正文。

### 第三层：工程审计附件（给复核与程序使用）

可详细记录 ASC、DBC 版本/hash、CAN ID/Signal、DLC
差异、采样周期、时间同步、对齐方法、原始窗口、峰值/均值/MAE、INVALID、解码失败、未知
ID、缺失 Signal、厂家阈值缺失、脚本、CSV/JSON 和不可外推项。

> **只有足以改变诊断结论的工程限制，才进入诊断正文。**

## 14. Codex 禁止行为

1.  用大量免责声明代替诊断判断；
2.  因缺厂家阈值就拒绝所有趋势判断；
3.  把驾驶输入直接等同最终控制请求；
4.  把不同状态窗口混合统计；
5.  把峰值之比直接当效率；
6.  把 CAN Signal 直接等同物理测量；
7.  把采样时间差直接称为内部控制延迟；
8.  看到相关性就宣布故障根因；
9.  为了完整把无关 Signal 塞入正文；
10. 让工程审计细节遮蔽实验问题。

## 15. Codex 必须行为

1.  先读取实验目的和诊断问题；
2.  明确验证的控制树片段；
3.  先识别状态，再比较 Signal；
4.  实际动作时间轴优先于脚本名义时间；
5.  区分输入、请求、执行、反馈、物理结果和能源响应；
6.  发现差异先检查操作和工况；
7.  对瞬态窗口分析时间结构；
8.  对能回答的问题明确回答；
9.  保留证据边界；
10. 保留可复现的数据与程序审计结果；
11. 正常基线与故障案例分开；
12. 只有现有控制树无法表达新证据时，才建议修改树结构。

## 16. 标准诊断总结模板

``` text
本次 [实验编号/工况] 验证了 [系统] 中 [控制树片段] 的动态对应关系。

[输入] 变化后，[请求] 随之变化；
[实际执行] 对 [请求] 表现为 [跟随/不跟随]。
执行后，[车辆/物理反馈] 与 [能源响应] 表现为 [一致/不一致]。

两次/多次表现差异 [可由驾驶输入或工况差异解释 / 无法由工况差异解释 / 尚不能区分]。
本次采集范围内 [未发现 / 观察到] [具体控制链异常]。

三个诊断问题：
① ______：是 / 否 / 证据不足。
② ______：是 / 否 / 证据不足。
③ ______：驾驶/工况差异 / 控制异常 / 尚不能区分。

证据边界：
[只写会影响上述结论的关键限制。]
```

## 17. 与实车故障诊断工作单的关系

数据分析报告不是最终现场工作单。故障诊断文档应能拿到车旁直接执行和填写，推荐：故障现象
→ 已知事实 → 系统/物理链路 → 控制关系 → 现场位置 → 基线/故障对比 →
分阶段诊断步骤 → 每步预期结果 → 实测记录 → 判断与下一分支 → 安全边界 →
已证实/未区分项 → 最终诊断结论。

原则：**证据不足，不先换件。**

## 18. 本规范在项目中的优先级

1.  **本规范**：诊断语言、推理规则、报告协议；
2.  **Notion L3
    学习资料**：系统知识、主线、控制树、状态机、故障树等背景；
3.  **车型资料 / DBC / 电路图**：车型实例化；
4.  **基线实验**：正常实车证据；
5.  **故障案例**：异常实车证据；
6.  **分析脚本**：计算与自动化；
7.  **单次报告**：按以上规则输出。

若单次输出与本规范冲突：**先按本规范重新组织分析，再检查是否存在足以修改规范的新证据。**
不得因为一次程序的输出习惯，反向改变已经稳定的诊断体系。

## 19. 每份报告的自检清单

-   [ ] 是否根据实验目的、控制树片段和状态机生成内部诊断问题，并完成证据与控制关系自检？
-   [ ] 是否将答案综合为“证据→判断→结论”，而非机械保留“三个实验问题”章节？
-   [ ] 若采用“问题—回答”，是否确为实验脚本明确要求逐项回答？
-   [ ] 是否明确验证的控制树片段？
-   [ ] 是否区分输入、请求、执行、反馈和结果？
-   [ ] 是否先识别状态再统计？
-   [ ] 是否把不同控制工况分开？
-   [ ] 是否明确控制链在哪一级正常或异常？
-   [ ] 是否优先检查驾驶/工况/数据差异？
-   [ ] 是否给出明确诊断判断，而不是只有免责声明？
-   [ ] 是否把工程审计细节放在正文之后？
-   [ ] 结论是否没有超出证据边界？
-   [ ] 正常基线与故障推断是否分开？
-   [ ] **人在报告第一屏能否知道：发生了什么、正常不正常、为什么？**

最后一项若为"否"，报告必须重新组织。

## 20. 结语

目标不是得到更多 CAN
数据，也不是生成更长的报告，而是：**从系统结构出发，通过主线理解作用关系，以控制树描述正常逻辑，以状态机确定工况边界，通过
Signal
和物理证据验证实际行为；当预期关系不成立时进入诊断树和故障树，最终形成可解释、可复现、可执行的新能源
L3 诊断方法。**

> **数据服务于控制关系，控制关系服务于诊断。**
